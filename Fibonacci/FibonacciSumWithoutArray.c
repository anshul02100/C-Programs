#include <stdio.h>
int fibo(int n)
{
    int x=0,y=1;
    int sum=0;
    sum=x+y;
    while(n>0)
    {
        
    }
}
void main()
{
    int n;
    printf("Enter the value of n\n");
    scanf("%d",&n);
    printf("Sum of fibonacci series upto %d terms = %d\n",n,fibo(n));
}